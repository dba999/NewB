# NewB
NewBalloon with setting up panels in flash pro cc
-----------------------------------------------------


The NewBalloon.html is generated by Flash when publishing because even if you uncheck 'overwrite html' flash create one if no html already have the same name than the project's name.

NewBalloonAlpha.html is where happens the magic.
